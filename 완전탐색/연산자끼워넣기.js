let input = ["2", "5 6", "0 0 1 0"];
let arr = input.map((e) => e.split(" "));
const numsArr = input[1];
const operator = input[2];

function sol(input) {  
  for (let i = 0; i < numsArr.length; i++) {
    for (let j = 0; j < )
  }
  console.log(arr)
}

function dfs()

sol(input);